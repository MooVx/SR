clear all;

fileID = fopen('in.txt', 'r');
saveFile = fopen('parse_data.txt', 'w');

A = fscanf(fileID,'%c');
data = uint8(str2num( A ));
character='0'
for i=1:16
    character= strcat(character,'1',dec2bin(A(i),8),'000')
end
character
i=1;
n=0;
int=1;

fprintf(saveFile, '%c', character);
fclose('all');